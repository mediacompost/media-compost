# The project website (website/mkdocs.yml). Not needed to run or develop the
# app — which is why it sits here rather than beside the requirements-*.txt
# files at the root, where every entry is something the app itself imports.
#
# Run from the REPOSITORY ROOT, like everything else here:
#
#   python3 -m venv .venv-docs
#   .venv-docs/bin/pip install -r website/requirements.txt
#   .venv-docs/bin/mkdocs serve -f website/mkdocs.yml --watch-theme   # :8000
mkdocs-material>=9.5
mike>=2.1
# The API reference, generated from the source. NOTE WHAT IS *NOT* HERE: none of
# `media_compost`'s own dependencies, and not the package itself. mkdocstrings
# reads Python through griffe, which parses the SOURCE rather than importing it,
# so the docs build needs neither sqlalchemy nor Pillow nor a torch stack — and
# CI does not install the app to document it. Verified rather than assumed: this
# venv has none of them and the reference builds.
mkdocstrings[python]>=0.27
