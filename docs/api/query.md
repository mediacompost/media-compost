# Querying

`lib.query()` returns an [`ItemSet`][media_compost.library.query.ItemSet]: lazy,
composable, and evaluated by the same code path the app's own grid uses.

See [Building conditions](conditions.md) for the condition models a query tree
is made of, when it comes from variables rather than from a string.

::: media_compost.library.query
