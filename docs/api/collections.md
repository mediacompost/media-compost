# Collections

What a handle's plural properties hand back — `item.tags`, `item.files`,
`lib.tags`. They behave like the built-in collection they are named after and
write through to the library.

::: media_compost.library._collections
