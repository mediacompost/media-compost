---
title: Home
template: home.html
hide:
  - navigation
  - toc
---

<!--
  The landing page lives in `website/overrides/home.html`, not here.

  It stopped being prose: a two-column hero with a screenshot gallery, a tabbed
  install panel, a card grid and four code panels is markup, and writing markup
  inside Markdown means every edit to it is an edit to indentation. The page's
  copy is in that template beside the structure it belongs to; its styles are in
  `docs/assets/site.css` and its two behaviours in `docs/assets/home.js`.

  This file is what puts the page at `/` and names it in the nav.
-->
